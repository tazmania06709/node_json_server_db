{
  "name": "proyecto-json-server",
  "version": "0.0.1",
  "description": "Armaremos un pequenno servifor con informacion de cursos",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json",
    "dev": "nodemon app.js"
  },
  "author": "Josman Vicens",
  "license": "MIT",
  "dependencies": {
    "json-server": "^1.0.0-beta.2"
  }
}
